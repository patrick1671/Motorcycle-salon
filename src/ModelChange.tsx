import PhotoChange from "./PhotoChange";
import {
  BrowserRouter as Router,
  Switch,
  Route,
  Link,
  useRouteMatch,
  useParams,
} from "react-router-dom";
const ModelChange = () => {
  return (
    /* <Router>
      <Link to="/about">G</Link>
      <Switch>
        <Route exact path="/about">
          <PhotoChange model={"G"} />
        </Route>
      </Switch>
    </Router>*/
    <></>
  );
};
export default ModelChange;
